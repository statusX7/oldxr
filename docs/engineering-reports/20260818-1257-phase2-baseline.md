# oldxr 第二阶段工程基线报告

## 基本信息

- 日期/时区：2026-08-18 12:57 BST（Europe/London，UTC+01:00）
- 阶段：PHASE 0，保护现场与建立第二阶段 baseline
- branch：`chore/phase2-baseline`（从 `main` 创建）
- commit / HEAD：`13ab12fa1882ff5deededb38c873bb99aab42162`
- remote：`origin = git@github.com:statusX7/oldxr.git`
- Go version：`go1.20.14 linux/amd64`
- module：`github.com/XrayR-project/XrayR`
- xray-core version：`github.com/xtls/xray-core v1.7.5`
- XrayR compatibility baseline：`v0.9.0`

## 测试环境

- OS：Debian GNU/Linux 12（bookworm）
- kernel：`Linux 6.1.0-9-amd64 x86_64`
- virtualization：KVM
- CPU：8 vCPU，AMD EPYC 9534
- RAM：15 GiB，可用约 14 GiB
- swap：8.0 GiB
- storage：160 GiB，总可用约 120 GiB
- architecture：`linux/amd64`
- soft open-file limit：1024
- Go root：`/root/projects/.tools/go1.20.14`
- module/cache protection：`GOFLAGS=-mod=readonly`，独立的 `/tmp/oldxr-*` cache

## Git 现场

- `main`、`origin/main` 均指向 `13ab12f`。
- `origin/HEAD` 指向 `origin/main`。
- 远端刷新后未发现 `master`。
- `v0.9.0` 指向 `23feaddb273c7c6d737f41ccab5e5375f4a63ede`，本阶段不得移动。
- 开始本阶段时唯一未跟踪文件是首次审计生成的 `AGENTS.md`；未发现未知生产源码修改。

## 问题背景与证据

本阶段要求在固定 XrayR v0.9.0、xray-core v1.7.5 和 V2Board 1.6.0 兼容契约下建立可持续的正确性、并发、性能和 Release 闭环。首次审计已经定位 P0 计费并发丢失、同 UID 属性变化、Controller 共享状态、Global IP/Device Limit 等候选问题；这些问题将在独立 branch、独立测试和独立报告中处理，本 baseline commit 不夹带生产修复。

当前发布链基线证据：

- `install.sh:18` 的 `REPO` 仍为 `statusX7/XR`。
- `XrayR.sh:89`、`XrayR.sh:115`、`XrayR.sh:281` 仍从 `statusX7/XR` 下载。
- `.github/workflows/merge-upstream.yml:41` 仍自动拉取 `gfw-fuck/XrayR`。
- `.github/workflows/release.yml` 仍监听 `master` 普通 push，Go 配置为 `^1.19`。
- `.github/workflows/release.yml` 构建时动态解析 v2fly geodata 最新 Release，产物不能完全复现。

以上仅是本阶段记录的待修复基线，不在规则/报告 commit 中顺手修改。

## 根因

PHASE 0 不处理产品根因。当前工程层根因是首次审计规则尚未纳入版本控制，且仓库没有统一的 `docs/engineering-reports/` 证据留存约定。

## 修改方案

1. 将首次审计形成的项目级 `AGENTS.md` 纳入正式版本控制。
2. 补充工程报告、maintenance tag、`main`/`master`、安装 channel 和 Release 验收的长期规则。
3. 建立 `docs/engineering-reports/` 并以本文件记录第二阶段真实起点。
4. 后续每个逻辑阶段使用独立工作分支、验证、报告和 commit。

## 修改文件

- `AGENTS.md`
- `docs/engineering-reports/20260818-1257-phase2-baseline.md`

oldxr 生产源码修改：0。

## 兼容性影响

无运行时影响。没有修改 Panel API、JSON field、traffic accounting、认证、限速、配置格式、Go module identity、依赖或 xray-core。

## 验证状态

### 正确性测试

不适用：本 commit 仅包含工程规则与报告。

### go test

本阶段尚未执行；首次审计基线为 `go test ./...` 被历史 `common/legocmd` 的旧 module import/未锁定依赖阻断。PHASE 1 将先复现，再以独立变更恢复全仓库验证。

### go vet

本阶段尚未执行；首次审计基线除 `common/legocmd` 阻断外，还存在测试代码中的 unkeyed `api.DetectResult` literal。PHASE 1 独立处理。

### race

本阶段尚未执行；首次审计已确认 selected core package 可进行 race 编译，全仓库 race 受 `common/legocmd` 阻断。PHASE 1 建立可重复集合。

### benchmark before / after

不适用：无生产性能修改。

### CPU / memory / allocation / GC

- CPU：未测 / 无可靠数据
- RSS：未测 / 无可靠数据
- heap：未测 / 无可靠数据
- `B/op`：未测 / 无可靠数据
- `allocs/op`：未测 / 无可靠数据
- goroutine：未测 / 无可靠数据
- GC：未测 / 无可靠数据
- mutex/block：未测 / 无可靠数据

### build

通过。实际命令使用 Go 1.20.14、`CGO_ENABLED=0`、`-trimpath` 和 `-mod=readonly` 构建 `./main`：

- output：`/tmp/oldxr-phase2-baseline`（不提交）
- size：125,221,498 bytes
- SHA256：`ce1cd318778c5f71c4a6c03d65ff1d808e4db09930652f1c1c39531be2784ffe`
- `go version -m`：`go1.20.14`、`linux/amd64`、`CGO_ENABLED=0`
- embedded xray-core：`v1.7.5`

## Release 状态

- maintenance release：尚未创建
- `master`：尚不存在
- release tag：尚未创建
- assets / SHA256：不适用
- fresh install / update：尚未验证
- 本阶段不创建 tag 或 Release。

## 已知风险

- 全仓库 test/vet/race 基线尚未恢复，不能把后续定向测试通过误报为全仓库通过。
- 发布链仍依赖旧仓库路径和漂移的 geodata；在 Release 自包含阶段完成前不得发布维护版。
- 当前 shell 的 soft `ulimit -n=1024` 不足以直接代表大量连接测试条件；后续只在隔离 test process 中调整并记录。
- 开发机 8 vCPU/15 GiB 的结果不能直接外推到 1C1G 生产节点。

## 未解决问题

- P0：traffic reporting lost-update window。
- P0：same UID modified user 可能残留旧 runtime identity。
- P0：Controller shared state 并发访问。
- P0/P1：Global IP/Device Limit map race、边界和 goroutine 放大。
- P1：Limiter retained state/per-connection allocation、V2Board ETag、global stats scan/stale counters。
- Release：验证基线、仓库引用、workflow、geodata、archive、installer/update/master/tag 均待完成。

## 下一步

进入 PHASE 1：在独立 branch 上复现并修复 `go test ./...`、`go vet ./...` 和核心 `go test -race` 基线；不升级 Go、xray-core 或依赖树，不改变生产行为来迁就错误 fixture。
