# oldxr v0.9.0-r1 正式发布与安装验收报告

## 基本信息

- 日期/时区：2026-08-18，Europe/London（UTC+1）
- 阶段：第一个优化维护 Release
- report branch：`release/v0.9.0-r1-docs`
- Release source commit：`b91d3c3ff567d40d4c02200d26dd26965909640b`
- publish-time `main`：`b91d3c3ff567d40d4c02200d26dd26965909640b`
- publish-time `master`：`b91d3c3ff567d40d4c02200d26dd26965909640b`
- `v0.9.0-r1`：`b91d3c3ff567d40d4c02200d26dd26965909640b`
- immutable baseline `v0.9.0`：`23feaddb273c7c6d737f41ccab5e5375f4a63ede`
- Go：1.20.14
- xray-core：v1.7.5
- Release URL：https://github.com/statusX7/oldxr/releases/tag/v0.9.0-r1

## Release Gate 结论

第一个 maintenance Release 的 required Gate 已通过：

- P0 traffic accounting correctness：已修复并有 bytes conservation/failed restore/race tests；
- same UID runtime update：已修复并覆盖 UUID/password/email/speed/device changes；
- Controller shared state race：已处理并有定向 race/stress；
- local/global DeviceLimit lost update、boundary 与 goroutine amplification：已处理；
- V2Board 1.6.0 config/user/submit fixture：通过；
- `go test ./...`、`go vet ./...`、`go test -race ./...`：通过；
- Linux amd64/arm64 build、package、archive layout、SHA256：通过；
- local isolated fresh install、update、`XrayR.sh update`、checksum failure：通过；
- public master/raw、public GitHub Release、online fresh/update/update-shell：通过；
- public Release binary transient systemd start/stop：通过；
- production release/download path 中 `statusX7/XR`：0 处；
- `v0.9.0` baseline tag：未移动。

GitHub-hosted Release/Docker workflow 的最终状态见“远程 Actions”章节。

## 正式 assets

### Linux amd64

```text
asset: XrayR-linux-64.zip
size: 26,428,962 bytes
SHA256: 52611697f4143c031a97690a3481abe06e066db827e02c93a32b8332dc20d600
binary size: 87,265,280 bytes
binary mode: 755
```

### Linux arm64

```text
asset: XrayR-linux-arm64-v8a.zip
size: 24,573,822 bytes
SHA256: 94100aaad09169b97abe1b4d5cd5096377d714b3fcf5b36cda9043f232a48776
binary size: 83,886,080 bytes
binary mode: 755
```

线上下载四个 asset/sidecar 后执行：

```bash
sha256sum -c XrayR-linux-64.zip.sha256 XrayR-linux-arm64-v8a.zip.sha256
```

结果均为 `OK`。

两种 binary 的 `go version -m` 均确认：

```text
Go 1.20.14
github.com/xtls/xray-core v1.7.5
CGO_ENABLED=0
vcs.revision=b91d3c3ff567d40d4c02200d26dd26965909640b
vcs.modified=false
```

amd64 `XrayR -version`：

```text
XrayR 0.9.0-r1 (A Xray backend that supports many panels)
```

amd64 clean commit 重复 build 与正式 candidate byte-for-byte 相同。arm64 无本机 runtime，只验证 cross-build、module metadata、archive 与 checksum。

## Archive layout

amd64/arm64 ZIP 均含 13 个文件：

```text
XrayR
XrayR.service
XrayR.sh
config.yml
dns.json
route.json
custom_inbound.json
custom_outbound.json
rulelist
geoip.dat
geosite.dat
README.md
LICENSE
```

installer required file 检查、binary executable mode、service path、manager script 与 config/data copy 均通过。

## 用户指定安装命令

正式命令：

```bash
bash <(curl -Ls https://raw.githubusercontent.com/statusX7/oldxr/master/install.sh) 0.9.0
```

为保护开发机，该命令的 installer body 和所有 public URL 均保持不变，只通过测试环境变量把 filesystem root 重定向到 `/tmp/oldxr-public-install.../root` 并使用 mock systemctl。真实输出确认：

```text
检测到架构：amd64（asset: linux-64）
maintenance channel 0.9.0 当前解析为 v0.9.0-r1
下载 statusX7/oldxr v0.9.0-r1：XrayR-linux-64.zip
XrayR-linux-64.zip: OK
XrayR 0.9.0-r1
oldxr v0.9.0-r1 安装完成
```

验收结果：

1. public raw URL HTTP 200：通过；
2. public `install.sh` SHA256 与 release source 完全一致：通过；
3. `0.9.0 → v0.9.0-r1`：通过；
4. download 只使用 `statusX7/oldxr/releases`：通过；
5. archive SHA256 verification：通过；
6. architecture asset `linux-64`：通过；
7. unzip/layout/config/data：通过；
8. binary version/VCS/xray-core：通过；
9. systemd service wiring：通过；
10. fresh install 不以无效示例 panel 盲目启动，等待管理员配置：符合设计；
11. update 保留已有 config 并 start/status：通过；
12. 下载/checksum 失败发生在替换前：通过。

## XrayR.sh update / update-shell

从 fresh install 得到的 packaged `XrayR.sh` 实际执行：

```bash
XrayR.sh update 0.9.0
```

结果再次从 public `statusX7/oldxr/master/install.sh` 进入 `0.9.0 → v0.9.0-r1`，线上 SHA256 通过，config 被保留，mock service `start` 和 `is-active` 成功。

`update_shell` 从以下 URL得到 HTTP 200：

```text
https://raw.githubusercontent.com/statusX7/oldxr/master/XrayR.sh
```

为避免覆盖开发机 `/usr/bin/XrayR`，目标重定向到 `/tmp`。下载文件、已安装 manager 与 release source 的 SHA256 均为：

```text
8f3020028f3ae5ab6af1c07a8f0656cb04988b688b80f5fb65a7fea74cd8fcec
```

## public binary systemd lifecycle

从 public Release fresh install 得到的 amd64 binary 以 `Nodes: []` fixture 运行在临时 systemd unit：

```text
ActiveState=active
SubState=running
ExecMainStatus=0
MemoryCurrent=16994304
TasksCurrent=10
```

随后停止成功。该测试证明 package binary 的 daemon lifecycle，不证明真实 panel/protocol traffic。

## 远程 Actions

- CodeQL run `32150346263`：`success`。
- Node.js 24 workflow maintenance 后的 CodeQL run `32152320703`：`success`。
- Build and Release run `32150623170`：validate 和全部 24 个 build jobs 为 `success`；最终 release job 因没有 checkout 且未设置 `GH_REPO`，`gh release upload` 报 `fatal: not a git repository`，故 run overall 为 `failure`。
- Publish Docker image run `32150623172`：`success`；从 `v0.9.0-r1` tag source 构建并推送 `linux/amd64`、`linux/arm64`、`linux/arm/v7`。

Docker workflow 实际发布：

```text
ghcr.io/statusx7/oldxr:v0.9.0-r1
ghcr.io/statusx7/oldxr:0.9.0
ghcr.io/statusx7/oldxr:latest
manifest digest: sha256:3566a893a0bfbf49307cc62ff0e411d47ff20f50c9a292e884dd7987f79365ff
```

公开 package 页面 `https://github.com/users/statusX7/packages/container/package/oldxr` 以匿名 HTTP 请求返回 200，并显示 `latest`、`v0.9.0-r1` 与同一 manifest digest。当前开发机没有 Docker/Podman，因此没有执行本地 `docker pull/run`；这一项只确认 GitHub-hosted build/push 与公开 package metadata。该三架构 Docker build 从 14:48:44Z 运行到约 15:29:30Z，QEMU 下目标架构原生编译耗时约 41 分钟，后续可独立研究 `BUILDPLATFORM/TARGETARCH` cross-build，但本次不改变已发布 tag source。

upload failure 不涉及编译或 artifact 内容。修复 commit `754cdf6` 在 release job 显式设置 `GH_REPO=${{ github.repository }}`。失败 run 的 24 组 artifacts（48 个 archive/sidecar）下载后逐组 `sha256sum -c` 全部通过；保留已验收 amd64/arm64 assets不覆盖，另外 22 个平台的 44 个文件由 `gh release upload` 受控补传。Release 当前共 48 个 assets。

tag workflow 运行时，GitHub 对 `actions/checkout@v4`、`actions/setup-go@v5`、`actions/upload-artifact@v4` 发出 Node.js 20 deprecated/forced Node.js 24 警告。该警告不影响本次 tag build，但不适合作为长期配置。

发布后 workflow maintenance commit 根据各官方 repository 的当前 major refs 和 `action.yml`（均明确 `using: node24`，CodeQL v4 为官方 composite action）更新为：

```text
actions/checkout@v7
actions/setup-go@v7
actions/upload-artifact@v7
actions/download-artifact@v8
github/codeql-action@v4
docker/setup-qemu-action@v4
docker/setup-buildx-action@v4
docker/login-action@v4
docker/metadata-action@v6
docker/build-push-action@v7
```

更新后的 workflow 已通过 `actionlint v1.6.27`。因为这是 tag 发布后发现的 CI runtime maintenance，不移动 `v0.9.0-r1`；最终 main/master 包含该修复。

远程 artifacts 还暴露了 ZIP metadata 的 timezone 可重复性问题：GitHub UTC runner 与 Europe/London 本地 build 的 binary SHA256 完全相同、ZIP size 相同，但 archive SHA256 不同。commit `a35eee0` 让 `scripts/build-release.sh` 显式 `TZ=UTC`；随后分别从 `TZ=Europe/London` 与 `TZ=UTC` 调用脚本，两个 archive byte-for-byte 相同。已发布的两个主 assets 保持原验收 hash，未被覆盖；后续 Release 使用固定 UTC metadata。

## 正确性、测试与 build

- `go test ./...`：通过；
- `go vet ./...`：通过；
- `go test -race ./...`：通过；
- `actionlint v1.6.27 .github/workflows/*.yml`：通过；
- Bash syntax：通过；
- workflow 声明的 24 个 target cross-build：通过；
- 远程 24 个 build jobs：全部通过；
- 远程 24 组 artifact sidecar：全部通过；
- MIPS/MIPSLE softfloat：通过；
- amd64/arm64 package：通过；
- isolated fresh/update/manager-update/checksum corruption：通过；
- public online install/update/update-shell：通过。

## 性能、内存与 GC

Release 链本身不修改 runtime hot path，不存在独立 before/after：

- `ns/op`：不适用；
- `B/op`：不适用；
- `allocs/op`：不适用；
- CPU before/after：不适用；
- heap/GC before/after：不适用；
- public empty-node daemon `MemoryCurrent`：16,994,304 B，仅作启动验收，不代表生产 RSS；
- mutex/block：未测 / 无可靠数据。

本 Release 包含的 runtime benchmark 与 1C1G synthetic 数据在前序专项报告和 phase2 final summary 汇总。

## V2Board 1.6.0 兼容性

Release source 没有升级 Go module、xray-core 或 XrayR baseline。V2Board legacy route、`node_id`、`token`、`data`、用户认证字段、traffic `user_id/u/d`、VMess `alter_id`、legacy `inbound/inbounds`、ETag/304 和 no-op status semantics 均由已有 fixture 保护。

未使用真实生产 panel credential，因此这是 source/fixture compatibility 验证，不是生产面板 smoke test。

## 已知风险

- fresh install 必须由管理员填写有效 panel 配置后启动；默认示例不自动启动。
- arm64 与其他 cross-build target 未在真实对应 OS/CPU 运行。
- Docker 三架构 build/push 已成功，但本机没有 Docker/Podman，未执行本地 pull/run smoke test。
- 真实 1C1G、真实协议/TLS/socket、生产网络与真实 V2Board panel 尚未执行端到端压测。
- `v0.9.0-r1` 指向 Release source `b91d3c3`；本报告作为发布后证据会进入后续 docs-only commit，tag 不移动。

## 下一步

1. 把本 Release 报告、Node.js 24 workflow maintenance 与 phase2 final summary 同步到 `main` 和 `master`，不移动 tag。
2. 在独立 VM 上补充真实协议、TLS、socket 与 V2Board mock panel 的长时间 1C1G 测试。
3. 用 native cross-build 优化 Docker 多架构构建时间，并补充目标架构 pull/run smoke test。
